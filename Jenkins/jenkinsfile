pipeline {
    agent any
    stages {
        stage("Clean workspace") {
            steps {
                cleanWs()
            }
        }
        stage("List Directory") {
            steps {
                sh 'ls -al'
            }
        }
    }
}
